import Logo from '../../assets/logo.png'

function Header(){

    return (
        <div>
            <img src={Logo} alt="logo-dev" style={{width:500 }} />
    
        </div>
    )
}

export default Header
